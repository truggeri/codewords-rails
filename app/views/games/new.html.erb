<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        Codewords
      </h1>
      <h2 class="subtitle">
        Play codewords with your friends online
      </h2>
    </div>
  </div>
</section>

<h1 class="title is-3">Start a new game</h1>

<div class="container">
  <%= form_for @game do |f| %>
    <div class="field">
      <%= label(:game, :name, class: "label") %>
      <div class="control">
        <%= f.text_field(:name, class: "input") %>
      </div>
    </div>

    <div class="field">&nbsp;</div>

    <div class="field">
      <div class="control">
        <button class="button is-link">Start Game 🚀</button>
      </div>
    </div>
  <% end %>
</div>